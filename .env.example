# Root env for docker-compose variable substitution (MySQL container).
# Copy to `.env` (gitignored) and fill in real values — never commit real secrets.
# App/backend secrets (JWT_SECRET, VAPID keys, etc.) live in backend/.env, not here.

MYSQL_ROOT_PASSWORD=change_me_root_password
DB_NAME=bengkel_motor
DB_USER=bengkel_motor
DB_PASS=change_me_db_password
